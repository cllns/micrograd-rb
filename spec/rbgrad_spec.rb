# frozen_string_literal: true

RSpec.describe Micrograd do
  it "has a version number" do
    expect(Micrograd::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
